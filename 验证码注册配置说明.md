# 91taffy.com 验证码注册 · 配置说明（2026-08-24）

> 前端验证码注册流程已上线（填邮箱/昵称/密码 → 发验证码 → 输 6 位码 → 自动登录）。
> 但 Supabase **内置邮件服务有硬限制**，必须配置自定义 SMTP 才能对真实用户发验证码。本文件是配置清单。

## 一、为什么必须配 SMTP？

Supabase 内置免费邮件服务（Built-in SMTP）对每个项目限制：

| 限制项 | 值 |
|--------|-----|
| 收件人范围 | **仅限项目团队成员**（Team 里的邮箱），其他人一律拒绝 |
| 发送频率 | **2 封/小时**（约等于无法用） |
| 稳定性 | 无 SLA，可能随时变动 |

刚才用 `test-otp-flow@qq.com` 实测，返回的就是 `429 over_email_send_rate_limit`。

**配置自定义 SMTP 之后**：
- 可以对任意邮箱发验证码
- 初始限流提升到 **30 封/小时**（可在 Rate Limits 页再调高）
- 可自定义发件人昵称，邮件更像自己站的（不会被 Supabase 共享域名拖累进垃圾箱）

## 二、准备工作：QQ 邮箱开 SMTP + 拿授权码（约 3 分钟）

1. 电脑浏览器登录 QQ 邮箱 → 右上角「设置」→「账号」
2. 找到「POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV 服务」区块
3. 把 **「SMTP 服务」** 打开（IMAP 顺手也开了也行）
4. 按提示发短信验证，会给你一个 **16 位授权码**（例如 `xxxxxxxxxxxxxxxx`）
   - ⚠️ 这不是 QQ 密码，是专门给第三方用的「授权码」
   - 复制保存好，下面 Supabase 配置要用

## 三、Supabase 控制台配置 SMTP（约 2 分钟）

1. 打开你的项目 → 左侧 **Authentication → SMTP Settings**（认证 → 邮件服务）
2. 打开 **Enable Custom SMTP** 开关
3. 填以下字段：

| 字段 | 填什么 |
|------|--------|
| Sender email（发件人） | `你的QQ号@qq.com`（和授权码对应那个） |
| Sender name（发件人昵称） | `91塔菲的小窝` |
| Host（主机） | `smtp.qq.com` |
| Port（端口） | `465` |
| SMTP username | `你的QQ号@qq.com` |
| SMTP password | 第二步拿到的 **16 位授权码**（不是 QQ 密码） |

4. 点 **Save** 保存
5. 可选：在 **Authentication → Email Templates** 里找到「One-Time Password」模板，
   把邮件标题/正文改成塔菲风格（正文里验证码会显示为 `{{ .Token }}`）

> 提示：QQ 邮箱 SMTP 用 465 SSL 端口；如果填 587 要勾选 STARTTLS。
> Supabase 在 Save 时会自动测试连接，连不上会立刻报错，不会白保存。

## 四、验证是否配好

1. 回到网站 https://91taffy.com 点「注册」
2. 填一个**你自己的**邮箱（不要用已注册过的）
3. 点「发送验证码」→ 等 1~5 秒
4. 打开邮箱（留意垃圾箱）收「One-Time Password」邮件，里面是 6 位数字
5. 输入 6 位码 → 点注册 → 自动进入登录态

- 若提示「验证码已发送」→ 邮件没到，看垃圾箱 / 等 1 分钟再发
- 若提示「操作太频繁」→ 同一邮箱 60 秒内不能重复发，等一会再试

## 五、配套说明

- **限流**：配好 SMTP 后默认 30 封/小时，对 91taffy 小站完全够用。
  若以后要调，在 **Authentication → Rate Limits** 里改 `Send OTP` 相关值。
- **免费额度**：QQ 邮箱 SMTP 免费，无额外费用，适合个人站起步。
- **已做的前端改造**（无需再操作）：
  - 注册弹窗改为「发验证码 → 输 6 位码 → 校验通过自动登录」
  - 发送按钮 60 秒倒计时防刷
  - 6 位码错误/过期/发送频繁等英文报错全部翻译成中文
  - 校验通过自动补建 profiles（走原 ensure_profile 兜底），第一个注册者仍是站长

# 91taffy.com 待执行 SQL 清单（2026-08-23 04:57）

> 方案A + 管理后台升级 都要去 **Supabase 控制台 → SQL Editor** 手动粘贴执行。
> 全部幂等，可重复执行，不会删已有账号。

## 1. 方案A：uid 延迟到邮箱验证后才分配（必须先执行！）
文件：`supabase/migrations/20260823060000_defer_uid_until_confirmed.sql`
- 效果：注册后**不再立即建号发 uid**，只有点击邮箱验证链接后才建 profiles 发 uid。
- 你之前试"还是先注册后验证"，就是因为这个 SQL 还没跑。
- 前端配套（注册提示去验证、登录 ensure_profile 兜底）早就做好了，跑完 SQL 立刻生效。

## 2. 管理后台升级
文件：`supabase/migrations/20260823070000_admin_user_manage.sql`
- 三个新功能依赖它：
  - 管理列表显示「邮箱认证状态 / 最后登录 / 封禁到期」
  - 站长点"详情"可重置密码
  - 站长/管理员可删除账户
- 包含三个 RPC：admin_list_users() / admin_delete_user(uuid) / admin_reset_password(uuid,text)

## ⚠️ 关于"查看用户密码"
Supabase 的密码是 **bcrypt 单向哈希**加密存储的，数据库里只有一串不可逆的哈希，**任何站长都无法查看明文密码**——这是正规网站的标准安全设计（能看明文密码的网站反而有大漏洞）。
所以我把"查看密码"做成了更实用的替代：
- **账号详情弹窗**：完整展示该用户邮箱、认证状态、封禁信息、最后登录、注册时间
- **重置密码**：站长直接给用户设一个新密码（例如用户忘密码时，站长重置后把新密码告诉用户，效果等同接管账号）

## 前端改动（已提交 git b0dcd2b，Vercel 会自动部署）
- 管理面板每行新增：「详情」「自定义天数封禁」「删除账户」按钮
- 点击「详情」弹出账号详情窗口
- 「自定义天数封禁」输入天数即封禁
- 删除账户有两次确认，不可恢复
- 若 RPC 没跑（迁移未执行），管理列表自动降级为基础 profiles 查询，功能不白屏
